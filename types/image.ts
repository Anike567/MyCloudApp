interface ImageType {
    id: number;           // Changed 'if' to 'id'
    image_location: string;    
    preview: string;
    device_id: string;     // Changed 'deviceId' to 'device_id'
}

export default ImageType;